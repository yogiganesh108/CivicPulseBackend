spring.application.name=backend
server.port=8081

# MySQL datasource (replace user/password/url as needed)
spring.datasource.url=jdbc:mysql://localhost:3306/civicpulse_db
spring.datasource.username=root
spring.datasource.password=21030-cm-108
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA (adjust as required)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Disable Flyway auto-migrations during local development when the database/version is incompatible.
# Remove or set to true in environments where Flyway migrations should run.
spring.flyway.enabled=false
# JWT (secret/key currently generated in JwtUtils; consider externalizing in prod)
# jwt.secret=change_this_in_production
# jwt.expiration-ms=86400000

### DEBUG / DIAGNOSTICS (temporary) ###
# Enable Spring Security debug logging to trace access decisions
logging.level.org.springframework.security=DEBUG
# Log incoming web requests (framework tracing)
logging.level.org.springframework.web=DEBUG
spring.main.web-application-type=servlet
